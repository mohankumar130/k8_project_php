---
- name: minikube server deployment
  hosts: minikube
  become: true
  remote_user: kmaster
  tasks: 
     - name: delete old deployment
       command: kubectl delete -f /home/ubuntu/project-php/apache.yml
    #  - name: delete old service
    #    command: kubectl delete -f /home/ubuntu/Service.yml
     - name: create new deployment
       command: kubectl apply -f /home/ubuntu/project-php/apache.yml
    #  - name: create new service
    #    command: kubectl apply -f /home/ubuntu/Service.yml